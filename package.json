{
  "name": "doodles",
  "version": "0.0.0",
  "private": true,
  "description": "Diagram authoring layer: parses Mermaid/PlantUML/BPMN/UML into a versioned doodle format, lays out via filigree, renders to SVG. Consumed by clouddiagram (Konva editor) and axonize (markdown viewer).",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "typecheck": "pnpm -r typecheck && tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
